# SoCEXy - Database project

- CEX server updates with "deltas" which represent changes to the system state
- Each delta represents a change in one or more variables (eg account balance / number of +/- pips)
